/** PostCSS config for Next.js — Tailwind v4 requires @tailwindcss/postcss (Vite plugin is not used by next build). */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
